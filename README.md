# 🔥 FIRE Engine — Система маршрутизации обращений колл-центра

> **⚠️ Важно для запуска:** `.env` файл не входит в репозиторий (в `.gitignore`).
> Файл будет передан отдельно перед демонстрацией — без него проект запустится но уже без ллм а стандартной логикой(потому что апи нет)

---

## Что это

Система автоматической маршрутизации обращений клиентов Freedom Finance.
Принимает тикеты из CSV, обогащает через AI (или rule-based fallback), распределяет по менеджерам по правилам: VIP-сегмент, язык обращения, тип, геолокация.

**Стек:** FastAPI · Streamlit · PostgreSQL · Docker · OpenRouter (Qwen) · Folium · Ngrok

---

## Быстрый старт

### 1. Получи `.env` файл и положи в корень проекта

```
OPENROUTER_API_KEY=sk-...
NGROK_AUTHTOKEN=...
```

### 2. Запусти Docker

```bash
docker-compose up -d --build
```

### 3. Загрузи данные через дашборд

Открой [http://localhost:8501](http://localhost:8501) → вкладка **«Загрузка CSV»** → загрузи три файла → нажми **«Запустить импорт»**

Или через терминал:
```bash
docker-compose exec api python run.py
```

---

## Адреса

| Сервис | URL |
|---|---|
| Дашборд | http://localhost:8501 |
| API (Swagger) | http://localhost:8000/docs |
| Ngrok (публичный) | http://localhost:4040 |

---

## CSV файлы

**tickets.csv** — GUID клиента, Пол, Дата рождения, Описание, Сегмент клиента, Страна, Область, Населённый пункт, Улица, Дом

**managers.csv** — ФИО, Должность, Офис, Навыки, Количество обращений в работе

**business_units.csv** — Офис, Адрес

---

## Логика маршрутизации

```
VIP / PRIORITY сегмент   →  менеджер с навыком VIP
Смена данных             →  только Главный специалист
Язык KZ или ENG          →  менеджер с соответствующим навыком
Нет подходящего в офисе  →  поиск в ближайшем офисе по геолокации
Нигде нет                →  CAPITAL_ESCALATION
```

---

## Полезные команды

```bash
# Статус контейнеров
docker-compose ps

# Логи API
docker-compose logs api

# Перезапустить дашборд
docker-compose restart dashboard

# Остановить всё
docker-compose down
```
